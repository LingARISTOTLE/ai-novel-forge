server:
  port: 8080

spring:
  application:
    name: novel-forge-backend
  datasource:
    url: jdbc:h2:mem:noveldb
    driverClassName: org.h2.Driver
    username: sa
    password: password
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
  h2:
    console:
      enabled: true

deepseek:
  api:
    url: https://api.deepseek.com/chat/completions
    key: sk-2b1cef35cf2e402581e8a754ccc6315b
    model: deepseek-chat
